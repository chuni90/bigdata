package mov.com.svc;

import mov.com.vo.AccountDAO;
import mov.com.vo.AccountDTO;

public class VoteService {

	public boolean vote(AccountDTO newAccount) {
		AccountDAO dao = new AccountDAO();
		int check = dao.voteMovie(newAccount);
		
		if (check <1)
			return false;
		else
			return true;
	}
}
