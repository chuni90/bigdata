[요청 디스패치 방식을 이용한 회원정보조회 프로그램]

웹에서 화면을 이용하는 기능
response.sendRedirect("이동할 url");

RequestDispatcher rdp = request.getRequestDispatcher("이동할 Url");
rdp.forward(request, response);


- 파일 구성 및 기능
	1) search_form.jsp
		- id를 입력받아 조회버튼을 클릭한다. 
		
	2) SearchMemberServlet.java : 요청 디스패치를 수행하는 서블릿(웹에서 action 역할을 하는 것이 servlet임)  
	
	
	3) MemberService.java
		- MemberDTO searchMemberById(String id) : id를 입력 받아 해당 회원의 객체를 리턴시켜주는 메서드
		- ArrayList<MemberDTO> showAllMember() : 등록된 전체 회원의 정보를 리턴시켜주는 메서드
	
	4) show_member.jsp
		요청받은 회원의 정보를 출력하는 파일
		
	5) MemberDTO.java
		- 한 명의 회원에 대한 정보를 객체화하는 클래스
		- id, pass, name, age
		
	6) showAllMemberServlet.java : 전체 회원의 정보를 출력하는 요청을 받아 처리하는 서블릿